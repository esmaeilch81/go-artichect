# go-artichect

A new Go project scaffold for `go-artichect`.

## Getting Started

1. Install Go (recommended version 1.22+).
2. Run the project:

```bash
go run .
```

## Notes

- Update `go.mod` with your module path if you plan to publish this package.
- Add application logic in `main.go` or create additional packages under `./pkg`.
